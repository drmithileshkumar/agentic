% Web version of the MyMathlibProject blueprint.
% Compile with:  leanblueprint web   (runs plastex -c plastex.cfg web.tex)

\documentclass{report}

\usepackage{amssymb, amsthm, amsmath}
\usepackage{hyperref}
% `nonreducedgraph` skips the transitive-reduction step, which is the only part
% of the build that shells out to the Graphviz `tred` binary — not installed
% here. The graph itself is rendered in the browser, so it still works. With
% Graphviz installed you can drop this option to get a reduced graph.
% `dep_by=chapter` draws one dependency graph per chapter instead of a single
% graph of the whole document. With 800+ benchmark statements the document-wide
% graph was an 878 KB page of disconnected dots; per chapter it is legible.
\usepackage[showmore, dep_graph, dep_by=chapter, nonreducedgraph]{blueprint}

\input{macros/common}
\input{macros/web}

% These must point at the repository this blueprint is actually published from:
% \github is what every "source" link on the site resolves against, and
% \dochome is where \lean{...} declaration links go.
\home{https://github.com/drmithileshkumar/agentic}
\github{https://github.com/drmithileshkumar/agentic}
\dochome{https://drmithileshkumar.github.io/agentic/docs}

\title{MyMathlibProject Blueprint}
\author{Anshika Jain}

\begin{document}
\maketitle

% ── Independent study chapters ───────────────────────────────────────────────
% These come from blueprint/src/chapters/ and precede the benchmark.
\input{chapters/environment}
\input{chapters/tutorial}
\input{chapters/tactic_cheat_sheet}
\input{chapters/minimum_viable_tactics}
\input{chapters/pipeline}
\input{chapters/mobile_llms}
\input{chapters/llm_in_use}

% ── Benchmark (auto-generated) ────────────────────────────────────────────────
% Body is generated from problems/candidates.jsonl by
% problems/gen_blueprint.py. Regenerate it with:
%   python problems/gen_blueprint.py
% Do not put content directly in this file — it would be lost on the next run.
\input{content}

\end{document}
